package com.dynamiclog.example;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Example Spring Boot application demonstrating Dynamic Log framework usage.
 */
@SpringBootApplication
public class DynamicLogExampleApplication {

    public static void main(String[] args) {
        SpringApplication.run(DynamicLogExampleApplication.class, args);
    }
}
